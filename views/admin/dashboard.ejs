<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Portfolio</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .dashboard-container {
            min-height: 100vh;
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
        }

        .dashboard-header h1 {
            background: linear-gradient(to right, var(--primary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .logout-btn {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
            padding: 0.6rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            border: 1px solid rgba(239, 68, 68, 0.3);
            transition: var(--transition);
        }

        .logout-btn:hover {
            background: rgba(239, 68, 68, 0.2);
        }

        .message {
            padding: 1rem;
            border-radius: 8px;
            margin-bottom: 2rem;
            text-align: center;
        }

        .success {
            background-color: rgba(34, 197, 94, 0.1);
            color: #22c55e;
            border: 1px solid rgba(34, 197, 94, 0.3);
        }

        .error {
            background-color: rgba(239, 68, 68, 0.1);
            color: #ef4444;
            border: 1px solid rgba(239, 68, 68, 0.3);
        }

        .forms-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .form-card {
            background-color: var(--card-bg);
            padding: 2rem;
            border-radius: 15px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .form-card h2 {
            color: var(--primary-color);
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .submit-btn {
            background: linear-gradient(to right, var(--primary-color), var(--secondary-color));
            color: #fff;
            padding: 0.8rem 2rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            width: 100%;
            transition: var(--transition);
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(56, 189, 248, 0.4);
        }

        .data-section {
            margin-top: 3rem;
        }

        .data-section h2 {
            color: var(--primary-color);
            margin-bottom: 1.5rem;
        }

        .data-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1rem;
        }

        .data-item {
            background-color: var(--card-bg);
            padding: 1rem;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .data-item h3 {
            color: var(--text-color);
            font-size: 1rem;
            margin-bottom: 0.5rem;
        }

        .data-item p {
            color: #94a3b8;
            font-size: 0.9rem;
            margin-bottom: 0.5rem;
        }

        .data-item-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .delete-btn {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
            border: 1px solid rgba(239, 68, 68, 0.3);
            padding: 0.4rem 0.8rem;
            border-radius: 6px;
            cursor: pointer;
            font-size: 0.85rem;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 0.3rem;
        }

        .delete-btn:hover {
            background: rgba(239, 68, 68, 0.2);
            transform: translateY(-1px);
        }

        @media (max-width: 768px) {
            .forms-grid {
                grid-template-columns: 1fr;
            }

            .form-row {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>

<body>
    <div class="dashboard-container">
        <div class="dashboard-header">
            <h1><i class="fas fa-tachometer-alt"></i> Admin Dashboard</h1>
            <div>
                <a href="/" class="btn" style="margin-right: 1rem;">View Portfolio</a>
                <a href="/admin/logout" class="logout-btn"><i class="fas fa-sign-out-alt"></i> Logout</a>
            </div>
        </div>

        <% if (success) { %>
            <div class="message success">
                <%= success %>
            </div>
            <% } %>

                <% if (error) { %>
                    <div class="message error">
                        <%= error %>
                    </div>
                    <% } %>

                        <div class="forms-grid">


                            <!-- Add Project Form -->
                            <div class="form-card">
                                <h2><i class="fas fa-plus-circle"></i> Add New Project</h2>
                                <form action="/admin/projects/add" method="POST" enctype="multipart/form-data">
                                    <div class="form-group">
                                        <label for="project-title">Project Title *</label>
                                        <input type="text" id="project-title" name="title" required
                                            placeholder="e.g., E-Commerce Platform">
                                    </div>
                                    <div class="form-group">
                                        <label for="project-description">Description *</label>
                                        <textarea id="project-description" name="description" rows="3" required
                                            placeholder="Brief description of the project"></textarea>
                                    </div>
                                    <div class="form-group">
                                        <label for="project-image">Upload Image</label>
                                        <input type="file" id="project-image" name="image" accept="image/*"
                                            onchange="previewImage(event)">
                                        <small style="color: #94a3b8; font-size: 0.85rem;">Max file size: 5MB. Supported
                                            formats: JPG, PNG, GIF, WebP</small>
                                    </div>
                                    <div id="image-preview" style="margin: 1rem 0; display: none;">
                                        <img id="preview-img"
                                            style="max-width: 100%; max-height: 200px; border-radius: 8px; border: 1px solid rgba(255, 255, 255, 0.1);">
                                    </div>
                                    <div class="form-group">
                                        <label for="project-image-url">Or use Image URL</label>
                                        <input type="url" id="project-image-url" name="imageUrl"
                                            placeholder="https://example.com/image.jpg">
                                        <small style="color: #94a3b8; font-size: 0.85rem;">If both file and URL are
                                            provided, file will be used</small>
                                    </div>
                                    <div class="form-group">
                                        <label for="project-tags">Tags (comma-separated) *</label>
                                        <input type="text" id="project-tags" name="tags" required
                                            placeholder="React, Node.js, MongoDB">
                                    </div>
                                    <div class="form-group">
                                        <label for="project-link">Project Link *</label>
                                        <input type="url" id="project-link" name="link" required
                                            placeholder="https://github.com/username/project">
                                    </div>
                                    <button type="submit" class="submit-btn">
                                        <i class="fas fa-plus"></i> Add Project
                                    </button>
                                </form>
                            </div>

                            <!-- Add Skill Form -->
                            <div class="form-card">
                                <h2><i class="fas fa-tools"></i> Add New Skill</h2>
                                <form action="/admin/skills/add" method="POST">
                                    <div class="form-group">
                                        <label for="skill-name">Skill Name *</label>
                                        <input type="text" id="skill-name" name="name" required
                                            placeholder="e.g., Java">
                                    </div>
                                    <div class="form-group">
                                        <label for="skill-category">Category *</label>
                                        <select id="skill-category" name="category" required
                                            style="width: 100%; padding: 0.8rem; background-color: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 8px; color: #fff;">
                                            <option value="Frontend">Frontend</option>
                                            <option value="Backend">Backend</option>
                                            <option value="Database">Database</option>
                                            <option value="Tools">Tools</option>
                                            <option value="Other">Other</option>
                                        </select>
                                    </div>
                                    <div class="form-group">
                                        <label for="skill-icon">Icon Class (FontAwesome) *</label>
                                        <input type="text" id="skill-icon" name="icon" required
                                            placeholder="e.g., fab fa-java">
                                        <small style="color: #94a3b8; font-size: 0.85rem;">Find icons on <a
                                                href="https://fontawesome.com/search?o=r&m=free" target="_blank"
                                                style="color: var(--secondary-color);">FontAwesome</a></small>
                                    </div>
                                    <button type="submit" class="submit-btn">
                                        <i class="fas fa-plus"></i> Add Skill
                                    </button>
                                </form>
                            </div>
                        </div>

                        <!-- Current Data Display -->


                        <div class="data-section">
                            <div class="data-section">
                                <h2><i class="fas fa-tools"></i> Current Skills (<%= skills.length %>)</h2>
                                <div class="data-grid" style="margin-bottom: 3rem;">
                                    <% skills.forEach(skill=> { %>
                                        <div class="data-item">
                                            <div class="data-item-header">
                                                <h3 style="display: flex; align-items: center; gap: 0.5rem;">
                                                    <i class="<%= skill.icon %>"></i>
                                                    <%= skill.name %>
                                                </h3>
                                                <form action="/admin/skills/delete/<%= skill._id %>" method="POST"
                                                    style="display: inline;"
                                                    onsubmit="return confirm('Are you sure you want to delete this skill?');">
                                                    <button type="submit" class="delete-btn">
                                                        <i class="fas fa-trash"></i>
                                                    </button>
                                                </form>
                                            </div>
                                            <p><span
                                                    style="background: rgba(56, 189, 248, 0.1); color: #38bdf8; padding: 0.2rem 0.5rem; border-radius: 4px; font-size: 0.8rem;">
                                                    <%= skill.category %>
                                                </span></p>
                                        </div>
                                        <% }); %>
                                </div>

                                <h2><i class="fas fa-project-diagram"></i> Current Projects (<%= projects.length %>)
                                </h2>
                                <div class="data-grid">
                                    <% projects.forEach(project=> { %>
                                        <div class="data-item">
                                            <div class="data-item-header">
                                                <h3>
                                                    <%= project.title %>
                                                </h3>
                                                <div style="display: flex; gap: 0.5rem;">
                                                    <a href="/admin/projects/edit/<%= project._id %>" class="delete-btn"
                                                        style="background: rgba(34, 197, 94, 0.1); color: #22c55e; border-color: rgba(34, 197, 94, 0.3); text-decoration: none;">
                                                        <i class="fas fa-edit"></i> Edit
                                                    </a>
                                                    <form action="/admin/projects/delete/<%= project._id %>"
                                                        method="POST" style="display: inline;"
                                                        onsubmit="return confirm('Are you sure you want to delete this project?');">
                                                        <button type="submit" class="delete-btn">
                                                            <i class="fas fa-trash"></i> Delete
                                                        </button>
                                                    </form>
                                                </div>
                                            </div>
                                            <p>
                                                <%= project.description.substring(0, 80) %>...
                                            </p>
                                        </div>
                                        <% }); %>
                                </div>
                            </div>
                        </div>

                        <script>
                            function previewImage(event) {
                                const file = event.target.files[0];
                                const preview = document.getElementById('image-preview');
                                const previewImg = document.getElementById('preview-img');

                                if (file) {
                                    const reader = new FileReader();
                                    reader.onload = function (e) {
                                        previewImg.src = e.target.result;
                                        preview.style.display = 'block';
                                    };
                                    reader.readAsDataURL(file);
                                } else {
                                    preview.style.display = 'none';
                                }
                            }
                        </script>
</body>

</html>